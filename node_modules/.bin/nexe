#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../nexe/bin/nexe" "$@"
  ret=$?
else 
  node  "$basedir/../nexe/bin/nexe" "$@"
  ret=$?
fi
exit $ret
